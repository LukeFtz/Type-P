export type reduxValues = {
  ovenConnected: boolean;
  ovenConfiguration: boolean;
  heatStatus: boolean;
  temperature: number;
  heatFinished: boolean;
  recycleStated: boolean;
  recycleFinished: boolean;
  heatCanceled: boolean;
  recycleCanceled: boolean;
};
